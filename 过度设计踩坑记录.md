# 过度设计踩坑记录

这份记录约束后续开发取舍。需求真值仍以 [需求设计.md](需求设计.md) 为准；这里记录已经踩过的偏移、成因与防复发检查。

## 已踩过的坑

| 偏移 | 后果 | 回到需求的做法 |
| --- | --- | --- |
| 让服务器调用争议判定，再把同一份全局快照覆盖所有客户端 | 单人输入、换行和删行也可能被提升为自己的争议；本人的正文视角被别人的结果盖掉 | 服务器按普通操作维护正式行链，并保存客户端明确抄送的主张；是否发主张、何时显示争议由各客户端自己的注意力与收包决定 |
| 看到单人争议后只在界面隐藏那一行，或只给删除/回车补特殊判断 | 底层仍会产生错误候选；换一种操作又复发 | 先检查「有没有收到他人的主张包」这条唯一进场条件。停笔使注意力消失，之后普通外来改动直接整合、不外发主张 |
| 把重连恢复推演成必须保存每一条普通操作原包，并在文章文档增加完整事件日志与序号 | 重复储存链表已表达的正文，带来双真值、迁移、容量上限与回放代码；服务端出现旧版/新版并行 | 当前正文由正式行链表保存，客户端申报的分歧由现有争议文档保存，本机另存未发送修改。晚加入者拿当前链与主张；只有先写出这三者无法恢复的最小反例，才讨论增加存储字段 |
| 为所有客户端追求同一瞬间的全局操作序号与绝对顺序 | 加锁和回放放大延迟、复杂度，却无法消除网络上的暂时差异 | 目标是最终一致：普通编辑允许短暂不同；消息送达与状态交换后，空闲客户端收敛到当前链，未决分歧保留各自视角与同一组主张。只保障单端输入因果、链表更新原子性和消息幂等，不承诺瞬时一致 |
| 为让服务器“不判争议”另造一套 500 多行的链表镜像 | 重写了已有的拼接、链校验、拷贝和幂等路径，维护成本超过行为本身 | 复用现有 `document.Doc` 的链表原语，增加显式普通同步入口；服务器只走该入口，不走自动开争议的 `SubmitWith` 路径。实验镜像不得接入正式 Hub |
| 用“用户插入”操作来同步新参与者产生的系统空行 | 空行被记成某位用户的插入出处，后续候选会包含并非该用户主张的内容 | 系统补空行只追加已有 ID 的空正式行；不写插入出处、作者主张或争议 |
| 把“每位新参与者补一条初始空行”误判为多余正文并删掉 | 新文档的参与者一入场便共用同一注意力行，普通起笔更容易互相触发主张 | 首次加入者各有一条初始空正式行，同一身份重连复用；这是一条正文行，但由系统创建，不算任何人的插入主张 |
| 口头放弃事件历史，却继续保留 `Bootstrap.Events`、转发序号和两份按操作 ID 的去重表 | 协议与内存仍暗示并维护一套操作历史，读代码的人会继续沿错误模型扩展 | 入场包只带当前行链与显式主张；批次序号只用于当前连接的 ACK。去重只保留重发所需的最小短期信息，删除失效字段与重复表 |
| 同时保留两套服务、用 mock 网页代替产品联调 | 测到的协议与真实客户端不一致，修复可能只存在演示页 | 最终只保留一套客户端可连接的服务；用调试夹具扮演「测试脚本」，由用户测试真实桌面客户端。mock 只用于静态布局，不作为协同验收证据 |
| 把普通改行、插入/粘贴和跨既有行选区替换混成一种“跨行编辑”，再用多个输入框拼接 | 样例第 4/5 行的候选含义混乱，键盘移动、连续回车与选区体验偏离普通编辑器 | 普通改行只改一条；插入/粘贴带锚点上下文与新增行；跨多条既有正式行的选区操作才是一份整段主张。整个文档保持一个连续编辑面 |
| 为争议额外涂色、堆叠分隔线和所有候选行号 | 候选比正文抢眼，也让本人正文与别人的写法难辨 | 只用中性斑马底；本人候选在原底色上略亮/略暗，候选之间一条淡分隔线；行号只给本人认为的正文，其余用光标色点 |
| 把协议字段、候选内部结构和故障细节直接写进用户界面 | 展示项目显得繁杂，也增加用户认知负担 | 界面只说用户能行动的事：正在同步、有人提出不同写法、已追随、修改暂未送达。不可预料的内部错误写日志并给可检索编号 |
| 为本地随机测试数据设计额外迁移和保全流程 | 拖慢修复且偏离真实风险 | 本地测试数据允许清空；产品正文与主张仍须有明确持久化和失败恢复路径，不能把测试便利当作产品规则 |
| 把整套前端或同步架构一次性交给外部代理自由探索，再仅凭“测试通过”认领完成 | 代理长时间空转，容易同时保留新旧路径；测试也可能只覆盖自己写出的假设 | 将任务限定为一个可验收的行为切面和少量文件，先由主代理核对需求与现有入口，提交前复查真实调用链、反例与多余实现 |

## 不再靠直觉扩充架构

每次想加协议字段、数据库文档类型、版本历史、全局锁或第二套服务前，先回答：

1. 哪个**具体用户操作时序**仅靠「当前正式行链 + 显式主张 + 本机待发送修改」无法正确恢复？写成最小双端测试。
2. 新结构由谁创建、谁消费、何时删除？是否与已有链表或主张形成第二个权威来源？
3. 在无争议时，连续输入、行首/行尾回车、删除与跨行选区是否仍像普通共享编辑器？停笔后收到外来改动是否仍是普通编辑？

没有失败用例，就不为可能出现的情况预存完整历史。需要处理某种网络或恢复边界时，先扩展最小行为测试，再改当前责任层。

## 固定验收切面

- A、B 同行输入：普通同步触及本端活跃行且异文时，只向外发本人主张；收到他人主张后，本端才显示争议。自己的包及服务器回放自己的包绝不使自己进争议。
- 新文档中 A、B 依次加入：各有初始行且没有争议；只有主动移动到同一行编辑时，才进入上面的同行时序。同一身份断线重连不再添行。
- 光标移开或停笔一分钟：注意力消失；此后他人的普通编辑直接整合，零主张包、零新争议。已有争议候选可以挂起保留。
- 行尾回车的插入挂上行；行首回车的插入挂下行。已同步插入按本端已应用的链位置堆叠，候选保留该处完整插入段。
- 追随请求到达目标客户端后自动确认；互追按时间戳决胜，链式追随最终指向仍在主张的人。追随成功后，跟随方放下该处注意力，直到自己再次动笔。
- 断线后用正式行链与争议文档恢复共享状态，同时保留本机待发送文字；运行中的夹具可重连、继续写、十秒后随机决定自己是否追随。验证只认真实客户端与夹具，不认 mock 网页。

## 当前实验性代码的清理要求

隔离工作树里曾试做 `internal/relay` 的完整事件日志、`cmd/relayserver` 第二服务、Mongo 文章「事件/序号」字段，以及 `internal/server/mirror.go` 的重复链表实现。这些只是被纠正前的实验实现，**不得作为最终方案合并或部署**。收敛实现时保留已经验证的客户端注意力、主张入场和界面改进，撤去完整操作日志、全局序号、重复链表与并行旧服务；随后重跑上面的验收切面。
