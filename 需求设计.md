# 协同编辑器（共享文档）项目设计

## 核心需求

- 多人在线实时编辑文本/md/代码
- 显示每个人的编辑光标与选中
- 服务器P2P打洞或转发确保实时性（优先打洞直连，再转发）
  - 只P2P光标等高度实时、准确度要求不高的部分
  - 主张和追随要留下来，走转发，得送到
- 无锁行级争议处理（共同编辑）

## 无锁行级争议处理

### 为何不锁

人类写文档是有心流的，当我对那边有想法时，别人刚好也有。此时应该保留各自心流，而非打断

### 争议从哪来

我认为这处该这样，你认为该那样，这处就有争议。这是人和人之间的事，不是服务器判出来的。

- 我只改我本地的。你发来的那份，我不拿去盖我自己的
- 我这份发出去，就是我的主张
- 同一处有两份还没撤回的主张，争议就在
- 谁先看见谁先把它摆出来。后看见的人加入这一场，不再另开一场
- 服务器把主张转给其他人，同一处的几份都留下。后到的包不能把先到的主张盖掉。服务器不选谁对
- 一个人只有一个输入光标。不同人的主张按人分开

### 怎样算还在这处

说的都是输入光标，不是鼠标。鼠标碰一下不算。

- 输入光标停在这处，人还在写，才算注意力在这
- 输入光标主动挪走，或者在原地超过一分钟没动，主张挂起，颜色变淡
- 挂起就是暂时离开。别人这时来写，不拉争议
- 挂起的主张还在，别人仍然可以接受
- 输入光标回到自己的主张上，挂起结束
- 不想参与了，把输入光标放到别的地方就行
- 输入法还在组字时不向外发。字上屏了再发

### 如何排

- 实时显示各自光标与选中，一般情况下用户主动避让
- 文档以斑马纹做背景，常态一行就是一行
- 真有争议时，我自己的主张在中间，颜色更亮或更暗，防止和别人混在一起
- 其他人按我这边先收到的顺序，上下上下排开
  - 自己在第n行，第一个进来的在n-1，第二个n+1，第三个n-2
- 各端谁先谁后可以不一样，画面不用对齐
- 斑马纹和这一处用同一种颜色，看成这一处被撑开，不是另一行
- 行号顺着链数，只有一个，显示在我自己的主张前面
- 别人的主张不显示行号，显示点。点的个数是追随人数，点用主张者的颜色，才知道是谁
- 我的行号旁边也带这个点，我能看见自己带着多少人

文档行数不够人分的时候，服务器按加入顺序补空行。第一个人在第一行行首，第二个人在第二行行首，依此类推。默认光标不叠在一起。自己主动移过去，可以。

### 追随

- 点别人的主张，气泡先问自己：接受他的，放弃我的？
- 不是互相追随的，要等对方点头才生效。点头前两边都能继续改自己的，行没有被锁住
- 你追我、我又追你，同时发生：看谁发出的时间戳更早。早的生效，晚的失败，不用干等对方点头。失败的一方同步对方的追随结果，再重新选
- 生效之后，别人眼里我的主张消失。这份先留着，争议还没结束
- 我再改自己这份，追随取消，重新加入争议
- 已经有人跟着我的，我去追随别人，带着他们一起过去
- 还在主张的人都追随到同一个人，争议结束，这行变回普通行
- 争议结束前，还在主张的行都留着
- 争议结束后，没人主张的行销毁
- **达不成一致的争议就留在文档里。** 先放着，大家开会

## 每行作为基本对象

### 为何不使用行号

- 粘贴进来一大段，全毁了
- 正常编辑或插入，下方行号都得顺延
- 无法作为集群统一标识

### 参考mongoDB，使用ObjectID

- 文章、正式行、争议，各自一个 ObjectID。在 Go 里生成，不必先问数据库
- 不把「第几行」存进行里。行号是顺着正式行的链数出来的
- 正式行永远只有一个后一行。争议不改这个指针
- 争议胜出、要插进正文时：新行的前后关系出生时就写好
- 旧行只改两处：前一行的后一行ID改成这段第一行，原来下一行的前一行ID改成这段最后一行
- 这段最后一行的前一行，仍是段里面的上一行

一次做完的操作，是一份主张，打成一个包。

- 粘贴：一个包。改当前行，并把后面新插的几行放在同一个包里
- 行首退格：合并。就是在编辑前一行，把这一行的字接上去，再销毁这一行
- 这一行已经是空的，再按退格：销毁。这行上还有别人没挂起的输入光标，不销毁，拉争议
- 光标挪到远处再改，是另一次操作，另一个包
- 发送可以攒 0.1 秒到 0.5 秒再批量出去，最长 0.5 秒。再长就没有实时性了
- 批量只是一起送。里面仍按这个人的操作顺序分开，不把几次操作并成一份主张

### 整段撞在一起

两个人同时往同一行后面插入，比的是这次插入的整段。不从这里往下，把原来的后文全部拿来比。

- 每人一份争议文档，`做法` 都是「插在后面」，`真实行` 是同一个
- 原来的后文还挂在正式行的「后」上，不写进争议
- 段里的行不拆开逐行配对。第一行碰巧写得一样，也不单独合并掉
- 接受就是整段留下，另一段丢掉
- 胜出的那段才拆成正式行，接上前后指针。另一份争议删掉

### 我没在写的时候

- 我这处没有主张，对方的更新直接进来
- 我这处有主张，对方和我不同，两份都留，进入争议
- 断网时写在本地的，连上再发出去。和线上不同就两份都留，不直接覆盖

## 技术栈

桌面客户端。

- 窗口和通信用 Go。用 Wails（Go 包一层系统网页壳的框架，Windows 上这层壳是 WebView2）把页面嵌进来
- 编辑界面用 Vite、Vue、TypeScript 画。一行一个输入框，排成一列。争议是多出来的输入框
  - 虚拟滚动，输入框隐藏边框、滚动条、背景色等。保证看不到多输入框实现，整体视觉上就是一篇完整文档
- 链、主张、发包、本地缓存放在客户端的 Go 里。页面只负责画，把操作交回 Go
- 服务端用 Go。正在编辑的那份以内存为准，停一下再写入 MongoDB
- 管理后台另做一套 Vite、Vue、TypeScript，对接服务端的接口
- 光标打洞以后再补。主张和追随现在就走转发

## 数据结构

一篇文章一个集合。打开这个集合，原始数据就能看懂这篇。

三种文档。

文章，一篇一个：

- `_id`：这篇的 ObjectID
- 标题
- 不存首行 ID

加载这篇，直接把集合全查出来，在内存里组。

- 正式行里，没有「前」的那一条就是首行。双向链两头自然是空的，不必再在文章上存一份首行 ID
- 文章文档和争议文档也没有「前」，不能当成首行
- 全查得到的先后不是阅读顺序。用 ID 做表，从首行顺着「后」走一遍，才是正文
- 正式行里只能有一条没有「前」。有两条，就是链断了，或者残段没删干净，这时哪条算首行不再确定

正式行。前后指针只串这些。

- `_id`
- 前：上一行的 ObjectID。首行为空
- 后：下一行的 ObjectID。末尾为空
- 内容：这一行的字

争议。也在这个集合里，但不进链。

- `_id`
- 真实行：关于哪条正式行
- 做法：`改这行`，或 `插在后面`。只有真实行的 ID，看不出是在改字还是在后面加段
- 人：谁的主张
- 内容：一组字符串。改一行就是一项，一次粘贴的多行都在这一份里
- 追随者

同一条正式行上，每人一份争议，`真实行` 填同一个 ID。

分出结果以后：

- `改这行`：把胜出的内容写进那条正式行，删掉这几份争议
- `插在后面`：把胜出的内容拆成正式行，接在真实行和原来的下一行之间，删掉争议
- 没分出结果的争议留在集合里

